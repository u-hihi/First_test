# First\_test

test



안녕하세요. 첫번째 테스트입니다. ㅋ

26.5.26 수정했습니다.

여기에는 무엇을 할까요?

GitHub의 사용법에 대해 알아봅시다.(초보자 가이드용)
Git:
GitHub:

먼저 git-scm.com에서 Git Bash를 깔아줍니다.(Git 설치)

그 다음 GitHub에서 계정을 만들어 줍니다.

Git Bash에 들어가 최초 설정을 해봅시다.
아래의 명령어를 치면 됩니다.
git config --global user.name "사용자이름"
git config --global user.email "내이메일@gmail.com"


1. GitHub에서 repository(저장소)를 만들어 줍니다.
2. 내 컴퓨터로 clone(복제)을 합니다.
add

## 내 컴퓨터에서 삭제
git branch -d test

## GitHub에서 삭제
git push origin --delete test


# main에서 수정했어요
# 테스트2 브랜치에서 수정했어요